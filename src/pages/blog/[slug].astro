---
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const blogEntries = await getCollection('blog');
  return blogEntries.map(entry => ({
    params: { slug: entry.slug }, 
    props: { entry },
  }));
}

interface Props {
  entry: any;
}

const { entry } = Astro.props;
const { Content } = await entry.render();
---

<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="description" content={entry.data.description} />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{entry.data.title}</title>
</head>
<body class="bg-gray-900 text-white">
  <nav class="bg-black shadow-lg border-b border-gray-700">
    <div class="max-w-4xl mx-auto px-8 py-4">
      <div class="flex justify-between items-center">
        <a href="/" class="text-xl font-bold text-white">
          🎯 Mindset schlägt Grosskonzern
        </a>
        <div class="flex space-x-6">
          <a href="/" class="text-gray-300 hover:text-white">Home</a>
          <a href="/blog" class="text-white">Meine Geschichte</a>
          <a href="/beweise" class="text-gray-300 hover:text-white">Beweise</a>
        </div>
      </div>
    </div>
  </nav>

  <article class="min-h-screen bg-gray-900 py-12">
    <div class="max-w-4xl mx-auto px-8">
      <header class="text-center mb-12">
        <div class="mb-4">
          <time class="text-sm text-gray-400">
            {entry.data.pubDate.toLocaleDateString('de-DE')}
          </time>
        </div>
        
        <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">
          {entry.data.title}
        </h1>
        
        <p class="text-xl text-gray-300 mb-8">
          {entry.data.description}
        </p>
        
        {entry.data.tags && (
          <div class="flex flex-wrap gap-2 justify-center">
            {entry.data.tags.map((tag) => (
              <span class="bg-blue-900 text-blue-200 text-sm px-3 py-1 rounded-full">
                {tag}
              </span>
            ))}
          </div>
        )}
      </header>

      <div class="bg-gray-800 border border-gray-700 rounded-lg shadow-lg p-8 md:p-12">
        <div class="prose prose-lg prose-invert mx-auto">
          <Content />
        </div>
      </div>

      <div class="mt-12 text-center">
        <a 
          href="/blog"
          class="inline-flex items-center text-blue-400 font-semibold hover:text-blue-300"
        >
          ← Zurück zu allen Artikeln
        </a>
      </div>
    </div>
  </article>
</body>
</html>
